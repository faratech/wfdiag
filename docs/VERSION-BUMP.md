# Version bumping

Moved: version bumping is documented in [`scripts/README.md`](../scripts/README.md) —
`bump-version.py` updates the five surviving version sources and `check-version-sync.py`
fails CI on drift. This file previously described the removed Tauri-era bump set.
