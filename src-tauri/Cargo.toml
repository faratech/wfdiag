[package]
name = "wfdiag-tauri"
version = "2.1.6"
description = "WindowsForum Diagnostic Tool"
authors = ["WindowsForum"]
edition = "2024"

[lib]
name = "wfdiag_tauri"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.5", features = [] }

[dependencies]
tauri = { version = "2.9", features = [] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
# Tauri plugins for v2
tauri-plugin-fs = "2.4"
tauri-plugin-dialog = "2.4"
tauri-plugin-clipboard-manager = "2.3"
tauri-plugin-shell = "2.3"
tokio = { version = "1.47", features = ["rt-multi-thread", "sync", "time", "macros"] }
anyhow = "1.0"
reqwest = { version = "0.12", default-features = false, features = ["json", "native-tls"] }
uuid = { version = "1.17", features = ["v4"] }
futures = "0.3"
async-openai = { version = "0.30", default-features = false, features = ["native-tls"] }
rand = "0.9"
base64 = "0.22"
url = "2.5"
keyring = "3.6"
dirs = "6.0"


# Windows-specific dependencies
[target.'cfg(windows)'.dependencies]
wmi = "0.18"
winreg = "0.55"
windows-core = "0.62"
windows-future = "0.3"
windows-collections = "0.3"
windows = { version = "0.62", features = [
    "Win32_Foundation",
    "Win32_Security",
    "Win32_Security_Cryptography",
    "Win32_System_Threading",
    "Win32_System_SystemInformation",
    "Win32_System_Registry",
    "Win32_System_Performance",
    "Win32_NetworkManagement_IpHelper",
    "Win32_NetworkManagement_Ndis",
    "Win32_Storage_FileSystem",
    "Win32_UI_Shell",
    "Win32_UI_WindowsAndMessaging",
    # Wdk for NtQuerySystemInformation (process enumeration)
    "Wdk_System_SystemInformation",
    # ProcessStatus for GetPerformanceInfo
    "Win32_System_ProcessStatus",
    # DXCore for NPU detection
    "Win32_Graphics_DXCore",
    # WinRT for Phi Silica detection
    "Win32_System_WinRT",
    # Library loader for dynamic DLL loading (Phi Silica)
    "Win32_System_LibraryLoader",
    # Foundation for async operations
    "Foundation",
    # ApplicationModel for Limited Access Features (LAF)
    "ApplicationModel",
] }

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "z"
strip = true
